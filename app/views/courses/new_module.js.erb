$.fancybox({
  fitToView	: false,
  width		: '75%',
  height		: 'auto',
  autoSize	: false,
  closeClick	: false,
  openEffect	: 'fade',
  closeEffect	: 'fade',
  openSpeed	: 200,
  closeSpeed	: 200,
  padding     : 0,
  content 	: "<%= escape_javascript(render 'courses/shared/module_form_create') %>"
});

<% if !params[:course].blank? %>
  $("input#course_module_title").val("<%= params[:course][:module][:title] %>");
<% end %>
